package Flatten;

public class StringReverse {
	
	
	public  static char[] reverseString(char[] arr, int pos,int len) {
		if(pos==len-1) {
			char[] c = new char[len];
			c[0]=arr[pos];
			return c;
		}
		else {
			char [] retArr = reverseString(arr,pos+1,len);
			retArr[retArr.length-1]=arr[pos];
			return tempArr;

		}
		//abc,0,3
		////abc,1,3
		//abc,2,3

		
	}

}
